server:
  port: 6661

spring:
  application:
    name: clean-langChain
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/clean_ops?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456

mybatis-flex:
  global-config:
    print-banner: true
    key-config:
      key-type: auto
